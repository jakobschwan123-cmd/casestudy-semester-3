# QM-Review BPMN 09 + 10 — 2026-09-02

Prüfer: Jakob Schwan (Qualitätsmanagement) · Gegenprüfer: Kilian Platter
Gegenstand: `bpmn/p09-reklamation.bpmn`, `bpmn/p10-retoure.bpmn` (Stand V3, Commit `2379820`)
Branch: `feature/bpmn-09-10-review`

## Geprüft

Strukturprüfung des BPMN-2.0-XML gegen die Konventionen aus `bpmn/README.md`
(XML-Wohlgeformtheit, Diagram Interchange, Start-/Endereignisse, Pool-Auflösung).
Die Prüfung mit `@camunda/linting` (`tools/lint.mjs`) war nicht möglich, weil die
npm-Abhängigkeiten in diesem Arbeitsverzeichnis nicht installiert sind.

| Kriterium | p09-reklamation | p10-retoure |
|---|---|---|
| Aktivitäten | 12 | 10 |
| Start- / Endereignisse | 1 / 1 | 1 / 1 |
| Message-Flows | 4 | 3 |
| Pools | 2 | 2 |
| Elemente ohne DI-Shape | 0 | 0 |
| Kanten ohne DI-Edge | 0 | 0 |
| `BPMNPlane` → Collaboration | ja | ja |

**Ergebnis: inhaltlich vollständig, keine Befunde.** Unbenannte Elemente sind
ausschließlich Merge- und Join-Gateways; die bleiben konventionsgemäß ohne Label
und sind kein Befund.

## Offen

- **Camunda-Modeler-Prüfung steht noch aus.** Alle zehn Dateien tragen
  `exporter="RepairFlow BPMN Generator"`, waren also noch nie im Modeler geöffnet
  und gespeichert. Aufgabe 1 aus `bpmn/README.md` ist damit für keine Datei
  erledigt — auch nicht für 09 und 10. Nächster Schritt: beide Dateien im Camunda
  Modeler (Camunda 8) öffnen, Problems-Panel prüfen, Layout gegenlesen, speichern.

## Befund an anderer Karte

- **`p05-ersatzteilreservierung.bpmn` hat 2 Start- und 2 Endereignisse.** Die
  Konvention in `bpmn/README.md` lautet „Je Diagramm ein Start- und ein
  Endereignis"; die übrigen neun Prozesse halten sie ein. Zuständig: Adrian
  Wenzler (BPMN 05), Prüfer Nina Sattler. Zur Klärung in der Review-Runde —
  entweder Modell anpassen oder die Konvention um den begründeten Sonderfall
  erweitern und in `doku/03-entscheidungen.md` festhalten.

## Zuständigkeits-Widerspruch (zur Klärung in der Review-Runde)

Drei Dokumente weisen die Prüfung von BPMN 09 + 10 unterschiedlich zu:

| Quelle | Aussage |
|---|---|
| `doku/02-team-und-rollen.md` | Maxi ist BPMN-Verantwortlicher für alle zehn Diagramme; Jakob ist Qualitätsmanager |
| `doku/protokolle/2026-09-02-sprint1.md` | Jakob prüft BPMN 09 + 10 |
| `bpmn/README.md`, Abschnitt „Aufgaben für Maxi" | Maxi öffnet und speichert alle zehn Dateien |

Ohne Klärung wird doppelt geprüft oder gar nicht. Vorschlag: die Sprint-1-Aufteilung
(je zwei Diagramme pro Person) gilt für diesen Termin, Maxi bleibt fachlich
verantwortlich und führt zusammen. Entscheidung nach Beschluss in
`doku/03-entscheidungen.md` aufnehmen.
